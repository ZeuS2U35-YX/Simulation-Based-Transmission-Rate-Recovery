# Backward-compatible wrapper for the former selected-trajectory command.
# The manuscript figure uses an example-only task-1 sampled B(t) path.
source(file.path("code", "07_generate_task1_comparison_figures.R"))
