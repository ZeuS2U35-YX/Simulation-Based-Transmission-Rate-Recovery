# This lockfile reproduces the archived plotting environment only. Historical
# fitting used a separate HPC library that includes pomp. Fitting, validation,
# and post-processing entry points therefore use `Rscript --vanilla` so this
# plotting profile cannot silently replace the computational environment.
source("renv/activate.R")
